// This file is generated when you set up your Thing in the IoT Cloud. It defines the IoT variables



/*#include <ArduinoIoTCloud.h>
#include <Arduino_ConnectionHandler.h>

// Define IoT variables
int fireNodeID;
int safeEntranceID;

// Define WiFi credentials
const char SSID[] = "YOUR_WIFI_SSID";
const char PASS[] = "YOUR_WIFI_PASSWORD";

void initProperties() {
  ArduinoCloud.setThingId("YOUR_THING_ID");
  ArduinoCloud.addProperty(fireNodeID, READWRITE, ON_CHANGE, NULL);
  ArduinoCloud.addProperty(safeEntranceID, READWRITE, ON_CHANGE, NULL);
}

WiFiConnectionHandler ArduinoIoTPreferredConnection(SSID, PASS);
*/